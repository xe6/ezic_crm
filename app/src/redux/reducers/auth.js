//IMPORT TYPES HERE

const initialState = {
    authenticated: false,
    user_data: null
}

const authReducer = (state = initialState, action = {}) => {
    switch (action.type) {
        default:
            return state
    }
}

export default authReducer;